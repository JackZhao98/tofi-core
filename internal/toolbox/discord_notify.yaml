name: Discord Notify
description: 向 Discord Webhook 发送消息

nodes:
  send_discord:
    type: api
    config:
      method: "POST"
      url: "inputs.webhook_url"
      headers:
        Content-Type: "application/json"
      body:
        content: "inputs.message"
        username: "Tofi Bot"