name: Slack Notify
description: 向 Slack Webhook 发送消息

nodes:
  send_slack:
    type: api
    config:
      method: "POST"
      url: "inputs.webhook_url"
      headers:
        Content-Type: "application/json"
      body:
        text: "inputs.message"